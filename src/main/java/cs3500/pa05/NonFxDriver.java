package cs3500.pa05;

/**
 * Driver that does not contain the javaFX Application import
 */
public class NonFxDriver {
  /**
   * The real entry method for the code, so that the jar file will build.
   *
   * @param args - command line arguments
   */
  public static void main(String[] args) {
    Driver.launch(Driver.class, args);
  }
}
